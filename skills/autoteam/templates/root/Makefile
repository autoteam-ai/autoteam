# 由 autoteam 生成。三个目标都要改成本项目的真实命令，改完后删掉 AUTOTEAM-TODO 行。
# gate.yml 只调 `make check`；deploy.yml、rollback.yml 调 `make deploy`。
.PHONY: check dev deploy

check: ## 全部检查：lint、类型检查、测试，一条命令跑完
	@echo "AUTOTEAM-TODO: 在 Makefile 的 check 目标里串起本项目的全部检查" >&2; exit 1

dev: ## 一条命令起环境，可以重复执行
	@echo "AUTOTEAM-TODO: 在 Makefile 的 dev 目标里写起环境的命令" >&2; exit 1

deploy: ## 部署当前提交（在 GitHub Actions 里执行）
	@echo "AUTOTEAM-TODO: 在 Makefile 的 deploy 目标里写部署命令" >&2; exit 1
