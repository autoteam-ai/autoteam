> 本项目的经验库，由 Planner 提出、由人批准。受 CODEOWNERS 保护，改动走 PR。
> Planner 每次开工先读这里；日常观察先写进 Multica 的「运营笔记」任务，稳定下来的才提到这里。

# 经验库

这里写**从代码里看不出来、而且犯过一次不想再犯**的东西。每条都要能指导一个具体决定，写不出"下次该怎么做"的就别写。

空着比写满好：这是给 agent 每次运行都要读的东西，臃肿会稀释真正重要的几条。

## 拆任务

<!-- 例：某类改动实际总是超过行数上限，要按 X 维度拆；某两个模块一起改必然冲突，不要放同一批 -->

## 验收

<!-- 例：某个功能在线上怎么验才算数；哪些验收标准写法事后证明是验不了的 -->

## 选人

<!-- 例：哪类任务交给哪个 agent 明显更好，依据是哪几次的成绩单 -->

## 参数为什么是现在这个值

 - 2026-09-24：`AUTOTEAM_CODEOWNERS_GATE` 从 `on` 调为 `off`；初期阶段优先迭代速率。退出条件由人决定。

<!-- 每次调 autoteam.conf 的参数，在这里记一行：从多少改到多少、因为观察到什么。
     没有这段，半年后没人敢动这些数字 -->

- 2026-09-21：试行期巡检 `0 */4 * * *` → `0 */5 * * *`，对齐 Claude 5 小时额度窗口；4 小时一次容易叠加巡检和派发任务，导致额度不足。每天 20 点到次日 0 点仍有一次 4 小时间隔。

## 踩过的坑

<!-- 例：某个命令的默认行为会造成什么后果（像 gh pr merge --auto 在没有闸门的仓库里会立即合并） -->
